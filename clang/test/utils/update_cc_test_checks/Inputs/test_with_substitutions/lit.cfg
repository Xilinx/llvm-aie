# A sample lit.cfg file, used to test that update_cc_test_checks.py -s
# is able to understand the substitution below.

config.substitutions.insert(0, ('%ccflags', '-triple x86_64-unknown-linux-gnu -emit-llvm'))
