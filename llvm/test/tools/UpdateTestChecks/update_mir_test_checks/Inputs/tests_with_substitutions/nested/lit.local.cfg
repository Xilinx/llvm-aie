# A sample lit.local.cfg file, used to test that update_mir_test_checks.py -s
# is able to understand the substitutions below, and the one in the parent
# folder.

config.substitutions.insert(0, ('%pass', 'none'))
